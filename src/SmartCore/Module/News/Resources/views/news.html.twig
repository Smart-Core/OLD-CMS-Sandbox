
{% for item in news %}
    <h1><a href="{{ cms_module_path(node, 'smart_module_news.item', {'slug': item.slug}) }}">{{ item.title }}</a></h1>
    <p><b>{{ item.created|localizeddate('long') }}</b></p>
    <p>{{ item.annotation }}</p>
{% endfor %}

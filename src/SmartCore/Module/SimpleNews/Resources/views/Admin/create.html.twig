{% extends '@SimpleNewsModule/Admin/index.html.twig' %}

{% block title %}Написать новость / {{ parent() }}{% endblock %}

{% block content %}

    {% if folderPath %}
        <a class="btn btn-default btn-small btn-sm" href="{{ folderPath }}" title="{{ app.request.host ~ folderPath }}" style="float: right;">
            <i class="icon-home"></i><span class="glyphicon glyphicon-home" aria-hidden="true"></span> Перейти на страницу новостей на сайте
        </a>
    {% endif %}

    <ul class="nav nav-pills" id="myTab">
        <li><a href="{{ path('smart_module.news_admin') }}">Список всех новостей</a></li>
        <li class="active"><a>Написать новость</a></li>
    </ul>

    <div class="box box-primary">
        <div class="box-header with-border">
            {% include '@SimpleNewsModule/Admin/_form.html.twig' with {'form': form} %}
        </div>
    </div>

{% endblock content %}

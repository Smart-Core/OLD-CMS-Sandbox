{% if not depth %}
    {% set depth = null %}
{% endif %}

{% if not template %}
    {% set template = 'knp_menu.html.twig' %}
{% endif %}

{% set menuItem = knp_menu_get('UnicatModule:CategoryMenu:tree', [], {
    'categoryClass': categoryClass,
    'css_class': css_class,
    'depth': depth,
    'routeName': routeName,
    'structure': structure,
}) %}

{{ knp_menu_render(menuItem, {
    'currentClass': 'active',
    'clear_matcher': false,
    'template': template,
}) }}

{% extends 'BlogModule::layout.html.twig' %}

{% block smart_blog_content %}
    <h1>{{ 'All tags'|trans }}</h1>

    <hr/>

    {% for data in cloud %}
        <p><a href="{{ path('smart_blog_tag', {'slug': data.tag.slug }) }}">{{ data.tag }}</a> ({{ data.weight }})</p>
    {% endfor %}

{% endblock smart_blog_content %}

{% set menuItem = knp_menu_get('SmartBlogBundle:Category:tree', [], {'categoryClass': categoryClass}) %}

{{ knp_menu_render(menuItem, {
    'currentClass': 'active',
    'clear_matcher': false,
}) }}
